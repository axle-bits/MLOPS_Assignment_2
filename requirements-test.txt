# Runtime deps plus what pytest needs. Used by CI, which runs the test suite but
# does not train models, so it avoids the heavier tooling in requirements-dev.txt.
-r requirements.txt
pytest==8.3.4
httpx==0.28.1
