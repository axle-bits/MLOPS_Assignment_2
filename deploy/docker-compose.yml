# deploy/docker-compose.yml
services:
  api:
    image: ${DOCKERHUB_USERNAME}/catsdogs-api:latest
    ports:
      - "8000:8000"
    volumes:
      - ../models:/app/models:ro
    restart: unless-stopped
