# Runtime dependencies for the inference service. Kept free of test/dev tooling
# so the published Docker image contains only what serving needs.
torch==2.12.0
torchvision==0.27.0
fastapi==0.115.6
uvicorn[standard]==0.32.1
pillow==11.0.0
python-multipart==0.0.19
numpy==1.26.4
prometheus-client==0.21.1
requests==2.32.3
